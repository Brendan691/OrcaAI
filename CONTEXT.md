# CONTEXT — 小鲸 OrcaAI 领域语言

> 本文件是项目的**领域词汇表**。所有代码、文档、提交信息、issue 都用这里定义的词。
> 统一语言是为了让人和 AI 都能顺着同一套概念导航代码,不各说各话。
> 工程技能(`improve-codebase-architecture` / `tdd` / `diagnose`)会读这份文件。

## 一句话定位

小鲸 OrcaAI 是一个**为航运知识管理优化过的通用知识管理引擎**:内核与领域无关,负责"采集 → 结构化 → 存储 → 检索 → 应用";航运只是当前加载的一个**领域包**。

换个领域包(法律、医药、金融),内核不动就能变成另一个行业的知识管理器。

## 核心概念(领域词汇)

- **知识条目 / Document** — 用户收藏的一份内容(网页文章、上传的文件、粘贴的文本)。有标题、来源、正文、标签。是系统处理的基本单位。
- **切片 / Chunk** — 一份 Document 按长度切开的片段。向量化和检索都以切片为单位,因为大模型和向量库对长文本处理有上限。
- **四维标签 / Tags** — 描述一份 Document 的四个维度:业务类型、地理区域、主题类别、事件性质。是"航运领域包"的一部分,不是内核固有的。
- **领域包 / DomainPack** — 一个行业的知识:标签体系(`tags.yaml`)、分类提示词、报告模板、关键词规则,打包在一起。当前只有 `maritime`(航运)。是本项目"通用 + 领域优化"定位的技术落点。
- **采集器 / Collector** — 把"某个来源的原始内容"变成标准 Document 的适配器。当前有网页采集器和文件采集器;小红书/抖音/公众号采集器是未来的领域外扩展点(见 ROADMAP)。
- **向量 / Embedding** — 把一段文本变成一串数字(向量),意思相近的文本向量也相近。这是"语义搜索"的基础。由 LLM 服务(通义千问)生成。
- **知识库 / KnowledgeBase** — 所有切片 + 向量 + 元数据的集合。物理上 = Chroma 向量库 + SQLite 元数据库。
- **混合检索 / HybridSearch** — 项目的核心算法。检索不只看向量相似度,而是四个维度加权融合:向量(语义)+ 关键词(精确)+ 时间(新优先)+ 标签(分类)。权重可用网格搜索优化。
- **自适应证据融合 / AdaptiveEvidenceFusion(AEF)** — 当前检索排序算法。它先分析查询的精确词项、时效需求、复杂度和显式标签,再按候选元数据可靠性动态调整四路信号;结合加权 RRF 降低分数量纲偏差,最后用 MMR 风格选择减少重复切片。固定四维权重保留为实验基线。
- **置信度自适应证据重排 / CARE** — AEF 后的可选第二阶段。专用 cross-encoder 对前 10 个候选重新打分,系统比较 AEF 与 cross-encoder 的 top-1/top-2 间隔,按冻结公式逐查询决定融合强度。无 Key、超时或响应异常时必须回退 AEF。
- **信号可靠性门控 / ReliabilityGate** — 只有证据字段真实可用时才给对应信号分配权重。例如没有来源发布日期时,时间权重必须为 0;收藏时间不能冒充发布时间。
- **证据校正 / EvidenceCorrection** — 用户开启联网搜索后,根据本地检索置信度分配本地/Web证据预算,去除跨源重复结果并限制同一文档的相邻切片数量。
- **检索增强问答 / RAG** — 用户提问时,先从知识库检索相关切片,再让 LLM **基于检索到的内容**回答,而不是凭记忆瞎编。保证答案有出处。
- **引用 / Citation** — 回答中的一个角标及其对应证据。引用必须绑定一个检索结果,包含稳定 ID、标题、摘要和来源链接;知识库引用还要携带切片定位器。
- **定位器 / Locator** — 从引用回到原始内容具体位置的信息。文本先使用字符范围和行号,PDF/Office 后续扩展页码与附件位置。
- **领域应用 / Generator** — 基于知识库产出的成品,如航运周报、风险预警、公约解读。是领域包的"输出侧"。

## 系统边界(模块地图)

```
采集入口                内核(领域无关)              领域包(可替换)
─────────           ──────────────────           ──────────────
Chrome 插件  ──┐
文件上传     ──┼─▶ Collector ─▶ 切片 ─▶ Embedding ─▶ KnowledgeBase
粘贴文本     ──┘                                         │
                                                         ▼
Streamlit 后台 ◀── RAG / HybridSearch ◀──────────── 检索
                        ▲                                │
                        │                                ▼
                   Generator ◀───────────────────── maritime 领域包
                   (周报/预警)                      (标签/提示词/模板)
```

- **内核**:Collector 抽象、切片、Embedding、KnowledgeBase、HybridSearch、RAG —— 不含任何"航运"字样。
- **领域包**:`domains/maritime/` —— 标签体系、分类提示词、报告模板。换包即换行业。
- **入口**:Chrome 插件(收藏)、Streamlit 后台(浏览/搜索/问答/管理)。

## 关键不变量(改代码不能破坏的约定)

1. **内核不认识"航运"**。任何航运专有的词、标签、提示词,只能出现在 `domains/` 里。内核里出现 "maritime/航运/船" = 架构泄漏。
2. **一份 Document 的切片共享同一份元数据**(标题、来源、标签、时间)。
3. **RAG 的回答必须能追溯到来源切片**。不允许无出处的生成。
4. **检索先召回、后自适应融合排序**:向量召回候选,再用 AEF 重排并选择低冗余证据,不跳过。
5. **CARE 只能增强、不能阻断检索**:cross-encoder 不可用时必须返回 AEF 结果;线上参数与最终确认实验保持一致。
6. **时间信号只使用来源发布日期**:`published_at` 缺失时权重归零;`created_at` 仅表示入库时间,不得用于判断内容新旧。
7. **本地优先**:默认配置下,不依赖任何外部服务即可启动(除 LLM API)。见 `docs/adr/0002`。
