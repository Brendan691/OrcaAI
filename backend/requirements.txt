# ============================================================
# 小鲸 OrcaAI — 本地开发依赖(零外部服务,适配 Python 3.13)
# 生产额外依赖见 requirements-prod.txt(见 docs/adr/0002)
# 版本策略:此处声明兼容下限;装成功后用 pip freeze 生成
#           requirements.lock 锁定精确版本(可复现)。
# ============================================================

# ---- Web 框架 ----
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.12

# ---- 数据库(本地默认 SQLite;生产切 Postgres 见 -prod)----
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20

# ---- 向量库 ----
chromadb>=0.5.20

# ---- AI / LLM(通义千问,OpenAI 兼容协议)----
openai>=1.55

# ---- 认证 ----
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<5

# ---- 文档解析(网页 + 轻量文件解析)----
beautifulsoup4>=4.12
lxml>=5.3
pypdf>=5.0
python-docx>=1.1
python-pptx>=1.0

# ---- 配置 / 校验 ----
python-dotenv>=1.0
pydantic>=2.10
pydantic-settings>=2.6
PyYAML>=6.0

# ---- HTTP ----
requests>=2.32
httpx>=0.27

# ---- 管理后台 ----
streamlit>=1.40

# ---- 测试 ----
pytest>=8.3
pytest-asyncio>=0.24
